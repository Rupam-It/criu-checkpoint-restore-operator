apiVersion: criu.org/v1
kind: CheckpointSchedule
metadata:
  name: trigger-interval
  namespace: default
spec:
  namespace: default
  selector:
    matchLabels:
      app: schedule-test
  triggers:
    interval: 5s
