apiVersion: criu.org/v1
kind: CheckpointSchedule
metadata:
  name: trigger-annotation
  namespace: default
spec:
  namespace: default
  selector:
    matchLabels:
      app: schedule-test
  triggers:
    onAnnotation: true
